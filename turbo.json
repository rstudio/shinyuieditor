{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "prod": {
      "inputs": ["src/**/*.tsx", "src/**/*.ts"]
    },
    "build": {
      "outputs": ["build/**"],
      "inputs": ["src/**/*.tsx", "src/**/*.ts"]
    },
    "type-check": {
      "outputs": [],
      "inputs": ["src/**/*.tsx", "src/**/*.ts"]
    },
    "test": {
      "outputs": []
    },
    "watch": {}
  }
}
