{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["build/**"],
      "inputs": ["src/**/*.tsx", "src/**/*.ts"]
    },
    "type-check": {
      "outputs": [],
      "inputs": ["src/**/*.tsx", "src/**/*.ts"]
    },
    "test": {
      "outputs": []
    },
    "watch": {}
  }
}
