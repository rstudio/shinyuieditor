---

import { internalLink } from '@/lib/utils';
interface Props {
  loc: string;
}

const { loc } = Astro.props;
---

<style>
  .how-to-video {
    width: 100%;
  }
</style>

<video class="how-to-video" controls autoplay loop>
  <source src={internalLink(loc)} type="video/webm;" />
</video>
