{
  "name": "util-functions",
  "main": "./src/index.ts",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "shared-configs": "*",
    "typescript": "^5.0.4",
    "vitest": "^0.30.0",
    "just-clone": "^6.1.1"
  },
  "scripts": {
    "type-check": "tsc -p ./ --noEmit",
    "test": "vitest run",
    "test-watch": "vitest"
  }
}
