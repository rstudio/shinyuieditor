{
  "private": true,
  "workspaces": [
    "inst/*"
  ],
  "packageManager": "yarn@3.3.0",
  "scripts": {
    "prod": "turbo run prod",
    "build": "turbo run build",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "watch": "turbo run watch"
  },
  "devDependencies": {
    "husky": "^8.0.2",
    "turbo": "^1.6.3"
  }
}
