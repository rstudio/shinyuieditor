{
  "private": true,
  "workspaces": [
    "inst/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "watch": "turbo run watch"
  },
  "devDependencies": {
    "husky": "^8.0.2",
    "turbo": "^1.6.3"
  }
}
